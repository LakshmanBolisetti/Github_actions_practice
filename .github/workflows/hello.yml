name: this is hello world sample code
on: workflow_dispatch
jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: fist workflow
        run: echo "Hello World"

      - name: second step
        run: echo "hello from second step"

      - name: Third step for multiple run commands
        run: |
        whoami
        ls
        printenv
        echo " hello hi"
